import {styled, Text, View} from 'tamagui';

export const Chip = styled(View, {
  paddingVertical: 2,
  paddingHorizontal: 6,
  borderRadius: 5,
  borderWidth: 1,
});

export const ChipLabel = styled(Text, {
  fontSize: 12,
});
